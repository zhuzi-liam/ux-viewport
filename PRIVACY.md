# UX Viewport 隐私说明

生效日期：2026-07-17

UX Viewport 不收集、上传、出售或共享任何个人信息、网页内容或使用数据，也不接入统计和广告服务。

扩展仅在用户主动点击扩展图标后，通过 `activeTab` 和 `scripting` 在当前网页注入尺寸控件并读取可视区尺寸。预置尺寸、默认项和控件位置通过 `storage` 保存在用户自己的浏览器中，不会发送到外部服务器。

如有问题，请通过本项目的 GitHub Issues 联系维护者。
